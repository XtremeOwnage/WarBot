apiVersion: v1
kind: Service
metadata:
  name: {{ include "warbot.fullname" . }}
  labels:
    {{- include "warbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "warbot.selectorLabels" . | nindent 4 }}
